<%= form_with(model: jasser, local: true) do |form| %>
  <% if jasser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jasser.errors.count, "error") %> prohibited this jasser from being saved:</h2>

      <ul>
      <% jasser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
