<h1>Jasser des Monats <br />
	<% for delta in [-2, -1, 0, 1, 2]
		next_month = @date + delta.months
	%>
		<span class="headlinks" style="font-size:<%= 100/2**delta.abs %>%; padding:<%= 20-5*delta.abs%>px;">
			
			<% if delta == 0 -%>
				<%=short_month(next_month) %>
			<% else -%>
				<%= link_to short_month(next_month), :action => "month", :date => next_month %>
			<% end -%>
		</span>
	
	<% end %>
</h1>
<%= render 'user_ranking_table' %>
